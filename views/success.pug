html
  head
    title Empower4Life Signups
    meta(charset="utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1, user-scalable=no")
    link(rel="stylesheet" href="/assets/css/main.css")
  body(class="landing is-preload")
    - var title = "Thank You";
    include header
    .outer-main
      main
        section(class="Main-content")
          p Sign up successful! We've sent the details to your email.
          if item.title
            h3 #{item.title}
          if start && end
            p #{start} - #{end}
          else if end
            p Deadline: #{end}
          p!= item.Notes
          p Signed up for: #{count}
          if comment
            p Comment: #{comment}
          p
            a(href=`/event/${event}` class='button-style button-style1') Sign up for another
      include footer