html
  head
    title Empower4Life Signups
    meta(charset="utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1, user-scalable=no")
    link(rel="stylesheet" href="/assets/css/main.css")
  body(class="landing is-preload")
    - var title = 'Register';
    include header
    .outer-main
      main
        section(class="Main-content")
          if loggedIn
            p You're already logged in!
            p
              a(href='/logout') Click here to log out
          else
            if errors
              .errors
                ul
                each error in errors
                  li #{error.msg}
            form.register(method="POST" action="/register")
              div.form-group
                label(for='name') Name: 
                input#name.form-control(type='text' name='name' inputmode='text' autocomplete='name' required value=name)
                label(for='email') Email: 
                input#email.form-control(type='email' name='email' inputmode='email' autocomplete='email' required value=email)
                label(for='phone') Phone (optional, for login): 
                input#phone.form-control(type='tel' name='phone' inputmode='tel' autocomplete='tel' value=phone)
                input(type='hidden' name='item' value=item)
              div.form-group
                button(type='submit' class='button-style button-style2') Register
            p(style='margin-top:1.5rem; max-width:640px;') By registering you agree to our 
              a(href='/privacy') Privacy Policy
              |  . We only send SMS messages with one-time login codes; message and data rates may apply. Reply STOP to opt out.
      include footer
